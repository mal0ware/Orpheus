"""Reimplemented music-theory knowledge — the in-key scaffolding layer.

Plain Python data + helpers (scales, diatonic chords, cadences, genre profiles). All
*reimplemented*, not copied from any GPL source. Pure data → trivially testable, and it
keeps the LLM diatonic instead of emitting out-of-key MIDI.
"""
